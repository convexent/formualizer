---
title: "TOROW"
description: "TOROW: Converts an array or range into a single row."
---

<FunctionPageSchema id="lookup/torow" />

## Summary

Flattens input values into one row, with options to ignore blanks/errors and
 to scan by row or by column.

## Remarks
 - `ignore` values are `0` keep all, `1` ignore blanks, `2` ignore errors, `3` ignore both.
 - `scan_by_column` defaults to FALSE, so values are read row by row.

## Examples

<Tabs items={["Flatten to row", "Scan by column"]}>
<Tab value="Flatten to row">
<FunctionSandbox
  title="Flatten to row"
  formula={`=TOROW({1,2;3,4})`}
  grid={{}}
  expected={"[[1,2,3,4]]"}
/>
</Tab>

<Tab value="Scan by column">
<FunctionSandbox
  title="Scan by column"
  formula={`=TOROW({1,2;3,4},0,TRUE)`}
  grid={{}}
  expected={"[[1,3,2,4]]"}
/>
</Tab>

</Tabs>

## Related functions

- [TOCOL](/docs/reference/functions/lookup/tocol)
- [HSTACK](/docs/reference/functions/lookup/hstack)
- [VSTACK](/docs/reference/functions/lookup/vstack)
- [SORT](/docs/reference/functions/lookup/sort)
- [CHOOSECOLS](/docs/reference/functions/lookup/choosecols)
- [CHOOSEROWS](/docs/reference/functions/lookup/chooserows)

## FAQ

### Does TOROW preserve row order by default?
Yes. The default scan order is row-major.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="lookup/torow" />
{/* [formualizer-docgen:function-meta:end] */}
