---
title: "TRANSPOSE"
description: "TRANSPOSE: Swaps rows and columns in an input range."
---

<FunctionPageSchema id="lookup/transpose" />

## Summary

`TRANSPOSE` returns a spilled array whose shape is the inverse of the source shape.

## Remarks
 - Input with shape `R x C` returns output shape `C x R`.
 - Empty input returns an empty spill.
 - Errors in source cells are preserved in transposed positions.

## Examples

<Tabs items={["Transpose a row into a column", "Transpose a 2x2 matrix"]}>
<Tab value="Transpose a row into a column">
<FunctionSandbox
  title="Transpose a row into a column"
  formula={`=TRANSPOSE(A1:C1)`}
  grid={{"A1":"10","B1":"20","C1":"30"}}
  expected={"[[10],[20],[30]]"}
/>
</Tab>

<Tab value="Transpose a 2x2 matrix">
<FunctionSandbox
  title="Transpose a 2x2 matrix"
  formula={`=TRANSPOSE(A1:B2)`}
  grid={{"A1":"1","A2":"3","B1":"2","B2":"4"}}
  expected={"[[1,3],[2,4]]"}
/>
</Tab>

</Tabs>

## Related functions

- [TAKE](/docs/reference/functions/lookup/take)
- [DROP](/docs/reference/functions/lookup/drop)
- [HSTACK](/docs/reference/functions/lookup/hstack)
- [COLUMN](/docs/reference/functions/lookup/column)
- [COLUMNS](/docs/reference/functions/lookup/columns)
- [ROW](/docs/reference/functions/lookup/row)

## FAQ

### What happens to errors inside the source array?
TRANSPOSE preserves error values and only changes their position in the output matrix.

### Does TRANSPOSE return a scalar for 1x1 inputs?
Yes. After transposition, a 1x1 result collapses to a scalar in this engine.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="lookup/transpose" />
{/* [formualizer-docgen:function-meta:end] */}
