---
title: "OCT2BIN"
description: "OCT2BIN: Converts an octal text value to binary text."
---

<FunctionPageSchema id="engineering/oct2bin" />

## Summary

Supports optional left-padding through the `places` argument.

## Remarks
 - Input must be octal text up to 10 digits and may be signed two's-complement.
 - Converted values must fall in `[-512, 511]`, or the function returns `#NUM!`.
 - `places` must be at least the output width and at most `10`, or `#NUM!` is returned.

## Examples

<Tabs items={["Convert octal to binary", "Convert signed octal"]}>
<Tab value="Convert octal to binary">
<FunctionSandbox
  title="Convert octal to binary"
  formula={`=OCT2BIN("77")`}
  grid={{}}
  expected={"111111"}
/>
</Tab>

<Tab value="Convert signed octal">
<FunctionSandbox
  title="Convert signed octal"
  formula={`=OCT2BIN("7777777777")`}
  grid={{}}
  expected={"1111111111"}
/>
</Tab>

</Tabs>

## Related functions

- [BIN2OCT](/docs/reference/functions/engineering/bin2oct)
- [OCT2DEC](/docs/reference/functions/engineering/oct2dec)
- [DEC2BIN](/docs/reference/functions/engineering/dec2bin)
- [BIN2HEX](/docs/reference/functions/engineering/bin2hex)
- [DEC2HEX](/docs/reference/functions/engineering/dec2hex)
- [DEC2OCT](/docs/reference/functions/engineering/dec2oct)

## FAQ

### Why does `OCT2BIN` return `#NUM!` for some octal inputs?
After decoding, the value must be within `[-512, 511]` to be representable in Excel-style binary output.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="engineering/oct2bin" />
{/* [formualizer-docgen:function-meta:end] */}
