---
title: "BITRSHIFT"
description: "BITRSHIFT: Shifts a non-negative integer right or left by a given bit count."
---

<FunctionPageSchema id="engineering/bitrshift" />

## Summary

Positive `shift_amount` shifts right; negative `shift_amount` shifts left.

## Remarks
 - `number` must be a whole number in `[0, 2^48)`.
 - Shift values are numerically coerced; large right shifts return `0`.
 - Negative shifts that overflow the 48-bit limit return `#NUM!`.

## Examples

<Tabs items={["Shift right by three bits", "Use negative shift to move left"]}>
<Tab value="Shift right by three bits">
<FunctionSandbox
  title="Shift right by three bits"
  formula={`=BITRSHIFT(32,3)`}
  grid={{}}
  expected={"4"}
/>
</Tab>

<Tab value="Use negative shift to move left">
<FunctionSandbox
  title="Use negative shift to move left"
  formula={`=BITRSHIFT(5,-1)`}
  grid={{}}
  expected={"10"}
/>
</Tab>

</Tabs>

## Related functions

- [BITLSHIFT](/docs/reference/functions/engineering/bitlshift)
- [BITAND](/docs/reference/functions/engineering/bitand)
- [BITXOR](/docs/reference/functions/engineering/bitxor)
- [BITOR](/docs/reference/functions/engineering/bitor)
- [IMDIV](/docs/reference/functions/engineering/imdiv)
- [IMPOWER](/docs/reference/functions/engineering/impower)

## FAQ

### Why can negative shifts in `BITRSHIFT` return `#NUM!`?
A negative shift means left-shift; if that left result exceeds the 48-bit limit, `#NUM!` is returned.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="engineering/bitrshift" />
{/* [formualizer-docgen:function-meta:end] */}
