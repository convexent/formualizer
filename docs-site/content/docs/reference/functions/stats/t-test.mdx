---
title: "T.TEST"
description: "T.TEST: Returns the p-value from a Student t-test comparing two numeric samples."
---

<FunctionPageSchema id="stats/t-test" />

## Summary

`T.TEST` supports paired, equal-variance two-sample, and unequal-variance (Welch) modes.

## Remarks
 - `tails` must be `1` (one-tailed) or `2` (two-tailed).
 - `type` must be `1` (paired), `2` (two-sample equal variance), or `3` (Welch).
 - Returns `#N/A` when paired mode arrays have different lengths.
 - Returns `#NUM!` or `#DIV/0!` for invalid setup or degenerate variance conditions.

## Examples

<Tabs items={["Two-tailed equal-variance test with identical samples", "One-tailed Welch test with identical samples"]}>
<Tab value="Two-tailed equal-variance test with identical samples">
<FunctionSandbox
  title="Two-tailed equal-variance test with identical samples"
  formula={`=T.TEST({1,2,3},{1,2,3},2,2)`}
  grid={{}}
  expected={"1"}
/>
</Tab>

<Tab value="One-tailed Welch test with identical samples">
<FunctionSandbox
  title="One-tailed Welch test with identical samples"
  formula={`=T.TEST({1,2,3},{1,2,3},1,3)`}
  grid={{}}
  expected={"0.5"}
/>
</Tab>

</Tabs>

## Related functions

- [T.DIST](/docs/reference/functions/stats/t-dist)
- [T.DIST.2T](/docs/reference/functions/stats/t-dist-2t)
- [T.INV](/docs/reference/functions/stats/t-inv)
- [T.INV.2T](/docs/reference/functions/stats/t-inv-2t)
- [BINOM.DIST](/docs/reference/functions/stats/binom-dist)
- [F.DIST](/docs/reference/functions/stats/f-dist)

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="stats/t-test" />
{/* [formualizer-docgen:function-meta:end] */}
