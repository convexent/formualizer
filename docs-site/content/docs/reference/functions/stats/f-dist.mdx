---
title: "F.DIST"
description: "F.DIST: Returns the F-distribution probability for `x` with numerator and denominator degrees of freedom."
---

<FunctionPageSchema id="stats/f-dist" />

## Summary

Use `F.DIST` for left-tail cumulative probabilities or density values in variance-ratio tests.

## Remarks
 - Set `cumulative` to non-zero for CDF mode, or `0` for PDF mode.
 - Requires `x &gt;= 0`, `deg_freedom1 &gt;= 1`, and `deg_freedom2 &gt;= 1`.
 - Returns `#NUM!` when any domain constraint is violated.
 - Invalid numeric coercions propagate as spreadsheet errors.

## Examples

<Tabs items={["F CDF with symmetric 2 and 2 degrees of freedom", "F PDF with symmetric 2 and 2 degrees of freedom"]}>
<Tab value="F CDF with symmetric 2 and 2 degrees of freedom">
<FunctionSandbox
  title="F CDF with symmetric 2 and 2 degrees of freedom"
  formula={`=F.DIST(1,2,2,TRUE)`}
  grid={{}}
  expected={"0.5"}
/>
</Tab>

<Tab value="F PDF with symmetric 2 and 2 degrees of freedom">
<FunctionSandbox
  title="F PDF with symmetric 2 and 2 degrees of freedom"
  formula={`=F.DIST(1,2,2,FALSE)`}
  grid={{}}
  expected={"0.25"}
/>
</Tab>

</Tabs>

## Related functions

- [F.INV](/docs/reference/functions/stats/f-inv)
- [F.TEST](/docs/reference/functions/stats/f-test)
- [BINOM.DIST](/docs/reference/functions/stats/binom-dist)
- [GAMMA.DIST](/docs/reference/functions/stats/gamma-dist)
- [LOGNORM.DIST](/docs/reference/functions/stats/lognorm-dist)
- [NEGBINOM.DIST](/docs/reference/functions/stats/negbinom-dist)

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="stats/f-dist" />
{/* [formualizer-docgen:function-meta:end] */}
