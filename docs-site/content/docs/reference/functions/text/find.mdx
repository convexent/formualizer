---
title: "FIND"
description: "FIND: Returns the 1-based position of one text string inside another."
---

<FunctionPageSchema id="text/find" />

## Summary

`FIND` is case-sensitive and does not interpret wildcard characters.

## Remarks
 - Search is case-sensitive (`"A"` and `"a"` are different).
 - `start_num` is 1-based and must be greater than `0`.
 - If no match is found, returns `#VALUE!`.
 - Errors in either argument are propagated.

## Examples

<Tabs items={["Case-sensitive match", "Case mismatch fails"]}>
<Tab value="Case-sensitive match">
<FunctionSandbox
  title="Case-sensitive match"
  formula={`=FIND("World", "Hello World")`}
  grid={{}}
  expected={"7"}
/>
</Tab>

<Tab value="Case mismatch fails">
<FunctionSandbox
  title="Case mismatch fails"
  formula={`=FIND("world", "Hello World")`}
  grid={{}}
  expected={"#VALUE!"}
/>
</Tab>

</Tabs>

## Related functions

- [SEARCH](/docs/reference/functions/text/search)
- [EXACT](/docs/reference/functions/text/exact)
- [TEXTBEFORE](/docs/reference/functions/text/textbefore)
- [CONCAT](/docs/reference/functions/text/concat)
- [CONCATENATE](/docs/reference/functions/text/concatenate)
- [LEFT](/docs/reference/functions/text/left)

## FAQ

### Do wildcard characters work in FIND?
No. FIND treats * and ? as literal characters and matches case-sensitively.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="text/find" />
{/* [formualizer-docgen:function-meta:end] */}
