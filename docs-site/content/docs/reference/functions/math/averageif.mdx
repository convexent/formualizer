---
title: "AVERAGEIF"
description: "AVERAGEIF: Returns the average of cells that satisfy a single criterion."
---

<FunctionPageSchema id="math/averageif" />

## Summary

`AVERAGEIF` tests each cell in `range`, then averages matching values from `average_range`
 (or from `range` when `average_range` is omitted).

## Remarks
 - Criteria support comparison operators and wildcard text patterns.
 - Non-numeric values in the averaged cells are ignored.
 - If no cells match, `AVERAGEIF` returns `#DIV/0!`.

## Examples

<Tabs items={["Average values greater than a threshold", "Average one range using criteria from another", "No matches returns divide-by-zero"]}>
<Tab value="Average values greater than a threshold">
<FunctionSandbox
  title="Average values greater than a threshold"
  formula={`=AVERAGEIF(A1:A3, ">20")`}
  grid={{"A1":"10","A2":"25","A3":"40"}}
  expected={"32.5"}
/>
</Tab>

<Tab value="Average one range using criteria from another">
<FunctionSandbox
  title="Average one range using criteria from another"
  formula={`=AVERAGEIF(A1:A3, "East", B1:B3)`}
  grid={{"A1":"East","A2":"West","A3":"East","B1":"10","B2":"40","B3":"20"}}
  expected={"15"}
/>
</Tab>

<Tab value="No matches returns divide-by-zero">
<FunctionSandbox
  title="No matches returns divide-by-zero"
  formula={`=AVERAGEIF({1,2,3}, ">5")`}
  grid={{}}
  expected={"#DIV/0!"}
/>
</Tab>

</Tabs>

## Related functions

- [AVERAGE](/docs/reference/functions/math/average)
- [AVERAGEIFS](/docs/reference/functions/math/averageifs)
- [SUMIF](/docs/reference/functions/math/sumif)
- [COUNTIF](/docs/reference/functions/math/countif)
- [COUNTIFS](/docs/reference/functions/math/countifs)
- [SUMIFS](/docs/reference/functions/math/sumifs)

## FAQ

### When does AVERAGEIF return #DIV/0!?
It returns #DIV/0! when no matching cells contribute numeric values.

### If average_range is omitted, what gets averaged?
The function averages matching numeric cells from the criteria range itself.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="math/averageif" />
{/* [formualizer-docgen:function-meta:end] */}
