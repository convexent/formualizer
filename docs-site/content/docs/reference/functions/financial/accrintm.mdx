---
title: "ACCRINTM"
description: "ACCRINTM: Returns accrued interest for a security that pays interest at maturity."
---

<FunctionPageSchema id="financial/accrintm" />

## Summary

`ACCRINTM` accrues from `issue` to `settlement` with no periodic coupon schedule.

## Remarks
 - Date inputs are spreadsheet serial dates and must satisfy `settlement &gt; issue`.
 - `rate` is an annual decimal rate (for example, `0.05` for 5%), and `par` must be positive.
 - `basis` codes: `0=US(NASD)30/360`, `1=Actual/Actual`, `2=Actual/360`, `3=Actual/365`, `4=European30/360`.
 - Return value is accrued interest amount in the same currency units as `par`.
 - With positive `rate` and `par`, the result is positive.

## Examples

<Tabs items={["One full 30/360 year", "European 30/360 half-year accrual"]}>
<Tab value="One full 30/360 year">
<FunctionSandbox
  title="One full 30/360 year"
  formula={`=ACCRINTM(DATE(2024,1,1), DATE(2025,1,1), 0.05, 1000, 0)`}
  grid={{}}
  expected={"50"}
/>
</Tab>

<Tab value="European 30/360 half-year accrual">
<FunctionSandbox
  title="European 30/360 half-year accrual"
  formula={`=ACCRINTM(DATE(2024,2,28), DATE(2024,8,28), 0.04, 1000, 4)`}
  grid={{}}
  expected={"20"}
/>
</Tab>

</Tabs>

## Related functions

- [ACCRINT](/docs/reference/functions/financial/accrint)
- [PRICE](/docs/reference/functions/financial/price)
- [YIELD](/docs/reference/functions/financial/yield)
- [DB](/docs/reference/functions/financial/db)
- [DDB](/docs/reference/functions/financial/ddb)
- [IPMT](/docs/reference/functions/financial/ipmt)

## FAQ

### Does `ACCRINTM` use coupon frequency?
No. It accrues directly from `issue` to `settlement` with no periodic coupon schedule.

### What causes `#NUM!`?
Invalid `basis`, non-positive `rate`/`par`, or `settlement &lt;= issue`.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="financial/accrintm" />
{/* [formualizer-docgen:function-meta:end] */}
