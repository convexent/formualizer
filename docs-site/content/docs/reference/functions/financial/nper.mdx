---
title: "NPER"
description: "NPER: Calculates the number of periods needed to satisfy a cash-flow target."
---

<FunctionPageSchema id="financial/nper" />

## Summary

Use this to solve term length when periodic rate, payment, and value constraints are known.

## Remarks
 - `rate` is the interest rate per period.
 - Cash-flow sign convention: at least one of `pmt`, `pv`, or `fv` should usually have opposite sign.
 - `type = 0` means payments at period end; `type != 0` means period start.
 - Returns `#NUM!` when inputs imply no finite solution (for example, invalid logarithm domain).
 - Returns `#NUM!` when both `rate = 0` and `pmt = 0`.

## Examples

<Tabs items={["Example", "Example"]}>
<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=NPER(0.06/12, -1798.65157545827, 300000)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=NPER(0, -250, 5000)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

</Tabs>

## Related functions

- [PMT](/docs/reference/functions/financial/pmt)
- [PV](/docs/reference/functions/financial/pv)
- [RATE](/docs/reference/functions/financial/rate)
- [FV](/docs/reference/functions/financial/fv)
- [ACCRINT](/docs/reference/functions/financial/accrint)
- [ACCRINTM](/docs/reference/functions/financial/accrintm)

## FAQ

### Why does `NPER` return `#NUM!` for some sign combinations?
If the logarithm domain is non-positive (or `rate=0` with `pmt=0`), there is no finite solution and `#NUM!` is returned.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="financial/nper" />
{/* [formualizer-docgen:function-meta:end] */}
