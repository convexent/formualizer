---
title: "NOMINAL"
description: "Converts an effective annual rate into a nominal annual rate."
---

<FunctionPageSchema id="financial/nominal" />

## Summary

This is the inverse-style transformation of `EFFECT` for a chosen compounding frequency.

## Remarks
 - `effect_rate` is annual effective yield; `npery` is periods per year.
 - `npery` is truncated to an integer before computation.
 - Sign convention is not cash-flow based; this function converts annual rate representation.
 - Returns `#NUM!` when `effect_rate &lt;= 0` or `npery &lt; 1`.
 - Result formula: `npery * ((1 + effect_rate)^(1/npery) - 1)`.

## Examples

<Tabs items={["Example", "Example"]}>
<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=NOMINAL(0.12682503013196977, 12)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=NOMINAL(0.08243216, 4)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

</Tabs>

## Related functions

- [EFFECT](/docs/reference/functions/financial/effect)
- [RATE](/docs/reference/functions/financial/rate)
- [DOLLARDE](/docs/reference/functions/financial/dollarde)
- [DOLLARFR](/docs/reference/functions/financial/dollarfr)
- [CUMIPMT](/docs/reference/functions/financial/cumipmt)
- [CUMPRINC](/docs/reference/functions/financial/cumprinc)

## FAQ

### Is `NOMINAL` an exact inverse of `EFFECT`?
It is the corresponding transformation for the same integer `npery`; both functions require positive rates and `npery &gt;= 1`.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="financial/nominal" />
{/* [formualizer-docgen:function-meta:end] */}
