---
title: "ACCRINT"
description: "ACCRINT: Returns accrued interest for a coupon-bearing security."
---

<FunctionPageSchema id="financial/accrint" />

## Summary

`ACCRINT` calculates interest from either `issue` or the previous coupon date up to
 `settlement`, depending on `calc_method`.

## Remarks
 - Date inputs are spreadsheet serial dates; `settlement` must be after `issue`.
 - `rate` is the annual coupon rate as a decimal (for example, `0.06` for 6%), and `par` is principal amount; both must be positive.
 - `frequency` must be `1` (annual), `2` (semiannual), or `4` (quarterly).
 - `basis` codes: `0=US(NASD)30/360`, `1=Actual/Actual`, `2=Actual/360`, `3=Actual/365`, `4=European30/360`.
 - `calc_method`: non-zero accrues from `issue`; `0` accrues from the previous coupon date.
 - Return value is in the same currency units as `par` and is positive for valid positive inputs.

## Examples

<Tabs items={["Accrue from issue date (default calc_method)", "Accrue from previous coupon (calc_method = 0)"]}>
<Tab value="Accrue from issue date (default calc_method)">
<FunctionSandbox
  title="Accrue from issue date (default calc_method)"
  formula={`=ACCRINT(DATE(2024,1,1), DATE(2024,7,1), DATE(2024,7,1), 0.06, 1000, 2, 0)`}
  grid={{}}
  expected={"30"}
/>
</Tab>

<Tab value="Accrue from previous coupon (calc_method = 0)">
<FunctionSandbox
  title="Accrue from previous coupon (calc_method = 0)"
  formula={`=ACCRINT(DATE(2024,1,1), DATE(2024,7,1), DATE(2024,10,1), 0.08, 1000, 2, 0, 0)`}
  grid={{}}
  expected={"20"}
/>
</Tab>

</Tabs>

## Related functions

- [ACCRINTM](/docs/reference/functions/financial/accrintm)
- [PRICE](/docs/reference/functions/financial/price)
- [YIELD](/docs/reference/functions/financial/yield)
- [DB](/docs/reference/functions/financial/db)
- [DDB](/docs/reference/functions/financial/ddb)
- [FV](/docs/reference/functions/financial/fv)

## FAQ

### When does `calc_method` change the result?
`calc_method=0` accrues from the previous coupon date; any non-zero value accrues from `issue`.

### Which inputs return `#NUM!`?
Invalid `basis`, non-positive `rate`/`par`, unsupported `frequency`, or `settlement &lt;= issue` return `#NUM!`.

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="financial/accrint" />
{/* [formualizer-docgen:function-meta:end] */}
