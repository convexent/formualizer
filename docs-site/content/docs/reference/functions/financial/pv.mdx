---
title: "PV"
description: "PV: Calculates present value from periodic cash flows at a fixed rate."
---

<FunctionPageSchema id="financial/pv" />

## Summary

Use this to discount a regular payment stream and optional terminal value back to time zero.

## Remarks
 - `rate` is the discount rate per period.
 - Cash-flow sign convention: inflows are positive and outflows are negative.
 - `type = 0` assumes payments at period end; `type != 0` assumes period start.
 - When `rate` is zero, present value is computed with simple arithmetic (no discounting).
 - Returns argument-related errors if coercion fails or an input is an error value.

## Examples

<Tabs items={["Example", "Example"]}>
<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=PV(0.06/12, 360, -1798.65157545827)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

<Tab value="Example">
<FunctionSandbox
  title="Example"
  formula={`=PV(0, 10, -500)`}
  grid={{}}
  expected={"null"}
/>
</Tab>

</Tabs>

## Related functions

- [PMT](/docs/reference/functions/financial/pmt)
- [FV](/docs/reference/functions/financial/fv)
- [NPER](/docs/reference/functions/financial/nper)
- [RATE](/docs/reference/functions/financial/rate)
- [ACCRINT](/docs/reference/functions/financial/accrint)
- [ACCRINTM](/docs/reference/functions/financial/accrintm)

## FAQ

### How does `type` change `PV`?
`type=0` discounts end-of-period payments, while non-zero `type` treats payments as beginning-of-period (annuity due).

## Runtime metadata

{/* [formualizer-docgen:function-meta:start] */}
<FunctionMeta id="financial/pv" />
{/* [formualizer-docgen:function-meta:end] */}
